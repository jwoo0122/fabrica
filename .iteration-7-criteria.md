# Iteration 7 — Hover interaction + stable NodeId identity (Requirements Criteria)

> **Status:** planned milestone definition (in-progress).
> This file is the original done-definition for Iteration 7. Iteration 6 (`.iteration-6-criteria.md`, live-reload dev-loop) is deferred; it is superseded in execution order by this document but remains on disk for a future iteration (sequencing housekeeping to be recorded in SPRINTS.md upon Iter 7 completion).
> For the completed-iteration index see [`SPRINTS.md`](./SPRINTS.md); for the public capability snapshot see [`README.md`](./README.md).

## Goal

Iteration 7 의 정의: **"포인터 호버(hover) 를 CEL 변수 `interaction.hovered_id` 로 노출하고, `Condition` 이 그 변수를 분기 조건으로 사용하는 최소 SDUI 인터랙션 루프를 native + web 양쪽에서 완성한다."**
Pure `Condition`-wrapping 모델 — Frame/Text/Image 의 `variants` 속성은 도입하지 않는다. Hover 상태 변화는 `resolve_scene` 을 재실행하여 scene tree 를 교체한다. 노드의 `NodeId` 는 분기 교체에도 불구하고 안정적으로 유지되어야 하며, 그 보장을 위해 `SceneNode` 에 `user_key` 를 추가하고 `flatten_recursive` 의 ID 생성을 해시 기반으로 전환한다.

- **관찰 가능 surface:** (a) AccessKit tree (native + web), (b) ARIA mirror DOM (web 만, hover 시 `class_name` 반영), (c) **JSONL 이벤트 로그 사이드카** (`FABRICA_HOVER_LOG=<path>` 로 gate — 상태 전이 한 줄씩 append). JSONL 은 macOS 와 web 양측의 공식 hover 검증 채널이다 (Q3=C, `accesskit_macos` upstream PR 하지 않음).
- **Condition 의 역할:** 분기 조건식이 `interaction.hovered_id == "btn1"` 같은 바인딩된 변수 참조를 지원해야 한다. 이를 위해 `sdui-cel` 의 `ExpressionEngine` trait 을 최소 확장 — evaluation-time context 에 변수를 주입. CEL 는 bare identifier 를 사용하며 `$` 접두는 쓰지 않는다 (<https://github.com/google/cel-spec/blob/master/doc/langdef.md#syntax>).
- **Scene SSOT 유지:** 포인터 이벤트는 runtime 에서만 소비되어 `interaction` scope 를 갱신한다. scene JSON 자체는 쓰지 않는다.
- **`sdui-runtime-wgpu` 변경 최소화:** 렌더러는 여전히 `FlatNode` 리스트만 소비. hover 해석은 runtime apps 와 `sdui-core`(+ 옵션 `sdui-input`) 에서 일어난다.

## Scope

### In-scope

- `sdui-core`
  - `SceneNode::Frame`, `SceneNode::Text`, `SceneNode::Image` 세 variant 에 `user_key: Option<String>` 필드 추가. serde 는 `#[serde(default, skip_serializing_if = "Option::is_none")]`. `SceneNode::Condition` 에는 **추가하지 않는다** (operator 는 resolve 시점에 사라지므로). (§T-1)
  - `resolve_scene` 의 Frame branch clone 코드 (`/Users/jinwoo/repos/fabrica/sdui-core/src/lib.rs:281-291`) 가 `user_key` 를 보존. (§T-1)
  - `flatten_scene` / `flatten_recursive` 의 ID 할당을 positional counter 에서 해시 기반으로 전환. `NodeId(0)` 은 `ROOT_NODE_ID` 예약 유지. (§T-2)
    - 키가 있는 노드: `hash(parent_path || ":k:" || user_key)` → `NodeId(nonzero u64)`
    - 키가 없는 노드: `hash(parent_path || ":i:" || sibling_index)` → positional fallback, 기존 Iter 5 동작과 수치는 다르지만 **결정론적**.
    - 경로 구성 시 부모가 키 보유면 `":k:" || user_key`, 아니면 `":i:" || sibling_index` 를 사용. ROOT 은 빈 시드.
  - `hit_test_scene(flat: &[FlatNode], x: f32, y: f32) -> Option<NodeId>` — `FlatNode.abs_x/abs_y/width/height` AABB 를 **끝에서 앞으로** 스캔하여 최초 hit 반환 (painter's-last-wins). `ROOT_NODE_ID` 는 hit 후보에서 제외. 좌표는 `flatten_scene` 의 절대 좌표계와 동일. 모듈 위치: `sdui-core::hit_test` (별도 crate `sdui-input` 은 본 iteration 도입하지 않음; §T-3).
  - 새 통합 테스트 파일 `sdui-core/tests/identity.rs` — `flatten_preserves_id_across_sibling_insertion` 포함 최소 2 개 케이스. (§T-1, §T-11)
  - 해시 함수: `std::collections::hash_map::DefaultHasher` — built-in, no new dep; `user_key` 충돌 시 u64 space 의 확률적 충돌은 본 iteration 에서 허용 (테스트 케이스는 명시적 반증 없음). (§T-2 evidence)
- `sdui-cel`
  - `ExpressionEngine::eval_bool` 시그니처를 **확장**. 호환성을 위해 새 메서드 추가 방식 채택:
    - 기존 `fn eval_bool(&self, expr: &CompiledExpr) -> Result<bool, ExprError>;` 유지 (deprecation 없이 "빈 context 로 호출" 래퍼로 재정의).
    - 신규 `fn eval_bool_with(&self, expr: &CompiledExpr, bindings: &ExprBindings) -> Result<bool, ExprError>;` 추가.
  - 신규 `pub struct ExprBindings` + `pub enum ExprValue { String(String), Int(i64), Bool(bool), Null, Map(HashMap<String, ExprValue>) }` (문자열/정수/불리언/Null + map). (§T-4)
  - `CelEngine::eval_bool_with` 구현은 `cel_interpreter::Context::default()` 에 `add_variable`(or `add_variable_from_value`) 로 각 바인딩 설치 후 `Program::execute`. Map 바인딩은 `cel_interpreter::objects::Value::Map` 으로 구성. (§T-4)
  - `Program` 재사용 — `compile` 결과 `CompiledExpr` 가 이미 재사용 가능하므로 호출자 (runtime) 가 캐싱하면 충분. 본 iteration 에서는 **호출자-측 캐시** (per-expression string `HashMap<String, CompiledExpr>`) 를 `app-native` / `app-web` 에서 각각 도입; `sdui-core::resolve_scene` 자체는 iteration 7 에서도 매 resolve 마다 compile 허용. (§T-5)
  - 신규 unit test 7 개 최소:
    1. string 변수 바인딩 후 `interaction.hovered_id == 'btn1'` → true
    2. 동일 식, `hovered_id` 를 다른 값으로 바인딩 → false
    3. `null` 바인딩 시 비교는 `Err(EvalFailed)` 가 아니라 false (CEL null-safe semantics 확인; 실제 결과를 문서화하여 테스트 기대값 고정 — §T-4 evidence)
    4. 기존 literal `true` 테스트가 `eval_bool` (no-context 래퍼) 에서도 그대로 통과
    5. 바인딩되지 않은 변수 참조는 `Err(ExprError::EvalFailed)`
    6. `ExprBindings` 빌더 API (`ExprBindings::new().with_string(k,v).with_null(k)`) 존재
    7. **`binds_map_and_field_access_works`**: `ExprBindings::empty().with_map("interaction", HashMap::from([("hovered_id".into(), ExprValue::String("btn1".into()))]))` + 표현식 `interaction.hovered_id == 'btn1'` → `Ok(true)`. 동일 표현식에 bindings 없이 (`eval_bool`) 호출하면 unbound 로 실패.
- `app-native`
  - `winit::event::WindowEvent::CursorMoved { position, .. }` 이벤트를 주 이벤트 루프에서 소비 → `(x: f32, y: f32)` 로 변환 → `hit_test_scene(&flat, x, y)` → `Option<NodeId>` 획득.
  - `hovered_id: Option<NodeId>` 를 `App` 에 보관. 변경 발생 시:
    1. `user_key` 역매핑 (`NodeId → Option<String>`) — `flatten_scene` 결과와 병렬로 관리되는 `HashMap<NodeId, Option<String>>` 구축.
    2. `ExprBindings` 에 `interaction` 맵 바인딩 (`hovered_id: <user_key as String | null>`). 키 없는 노드 위에 있으면 `null`.
    3. `resolve_scene(&raw_scene, &engine, &bindings)` 재호출 → 새 resolved scene. `flatten_scene` 재생성. AccessKit `TreeUpdate` push. `window.request_redraw()`.
    4. 이전/새 상태가 다르면 JSONL sidecar 에 한 줄 append. (§T-6, §T-9)
  - `CursorLeft` 이벤트 → hovered_id = None, 동일 경로.
  - `WindowEvent::Resized` 시 hover 재평가는 하지 않는다 (Iter 8+ 과제).
  - `FABRICA_HOVER_LOG` env var 가 설정되어 있으면 해당 경로를 `OpenOptions::new().create(true).append(true).open(...)` 로 열고, `hover_enter`/`hover_leave` 전이마다 한 줄 JSON 을 append + flush. 경로 없으면 no-op.
  - `resolve_scene` 은 `interaction.hovered_id` 변수가 필요한 `Condition` 식을 해석해야 하므로, 엔진 호출 사이트 (`sdui-core/src/lib.rs:305-325`) 가 bindings 를 받도록 확장 — §T-7 참조.
- `sdui-core::resolve_scene` (시그니처 확장)
  - 신규 시그니처: `pub fn resolve_scene<E: ExpressionEngine>(root: &SceneNode, engine: &E, bindings: &ExprBindings) -> Option<SceneNode>`.
  - 호환성 래퍼: `pub fn resolve_scene_no_bindings<E: ExpressionEngine>(root: &SceneNode, engine: &E) -> Option<SceneNode>` 를 `resolve_scene(..., &ExprBindings::empty())` 로 정의 — 기존 호출자 (iteration 5 smoke) 가 최소 변경으로 넘어가도록.
  - 내부 `resolve_condition_branch` 는 `eval_bool_with(&compiled, bindings)` 호출. (§T-7)
- `app-web`
  - `canvas` element 에 `pointermove` / `pointerleave` 리스너 등록. `PointerEvent::client_x/y` 에서 canvas 의 `getBoundingClientRect()` 를 빼 scene 좌표로 변환. (§T-8)
  - native 와 동일한 hover_id 변환 → ExprBindings 재생성 → resolve_scene 재호출 → mirror DOM 재마운트 → `request_one_redraw`.
  - JSONL sidecar: web 에서는 `FABRICA_HOVER_LOG` 파일 쓰기가 불가능하므로 두 경로 병행 — (a) `console.log(JSON.stringify(line))` 로 로그 stream, (b) `window.__fabrica_hover_log = []` 배열에 push (agent-browser 가 읽을 수 있게). (§T-9)
  - Mirror DOM 노드에 hover 상태 반영: hovered 노드의 mirror `<div>` 에 `class="hovered"` attribute 부여, 이전 hovered 제거. ARIA role/label 는 변경 없음.
- `examples/hover-demo/`
  - `scene.json`: 최상위 Frame → child Frame (`user_key: "btn1"`, `label: "btn1-frame"`, 고정 크기/위치) 내부에 `Condition { when: "interaction.hovered_id == 'btn1'", then: Frame(빨강), else: Frame(파랑) }` 이 background swap 역할. `then`/`else` Frame 에는 `user_key` 를 넣지 않는다 (교체 대상이므로 positional fallback 테스트도 겸함).
  - `README.md`: Manual test recipe — pointer in/out, JSONL tail, CEL 식 수정 예시.
  - `golden/native.png`, `golden/web.png` — **초기 hover-off 상태만**. hover-on 상태는 JSONL 과 AX tree 로 검증하므로 golden PNG 대상 아님.
- `xtask`
  - `run-native` / `run-web` 이 `--example hover-demo` 지원. `build_web_dist` 가 이 예제의 `scene.json` 을 dist 로 copy.
  - 신규 서브커맨드 **없음**.
- `.claude/skills/verify-native/SKILL.md`
  - 신규 섹션 **Step: hover probe** 추가. 절차는 §AC-9 에 명시. cliclick 로 포인터 이동 후 JSONL tail + AX tree diff. (§T-10, §T-12)
- `.claude/skills/verify-web/SKILL.md`
  - 신규 섹션 **Step: hover probe**. agent-browser 로 pointer event dispatch + mirror DOM class assertion + `window.__fabrica_hover_log` assertion. (§T-10, §T-12)
- `README.md` / `SPRINTS.md` / `CLAUDE.md` — iteration 7 entry; architecture summary 에 "hover via resolve_scene re-run, JSONL sidecar, user_key-based stable IDs" 한 줄 추가. (§T-13)

### Out-of-scope (explicit deferred — Iter 8+)

- **Animation (including visual-only GPU uniform tweens).** hover 전이는 instantaneous frame swap 으로만 일어난다.
- **`variants` property on `Frame`/`Text`/`Image`** — Q1=C 결정. Iter 7 은 순수 `Condition`-wrapping 만 허용.
- **`resolve_scene` memoization / diff-based TreeUpdate** — 현재는 매 hover change 마다 full resolve + flatten + full `TreeUpdate::build`. 성능 최적화는 Iter 8+. (§T-7 note)
- **`accesskit_macos` upstream PR** — Q3=C 결정. 상류 변경 없이 JSONL sidecar 만으로 검증.
- **Press / focus / drag / scroll / keyboard / gesture** — 모두 future iteration.
- **ForEach / Fetching / PresetRef 연산자** — future.
- **Preset library / editor UI / scene graph editor** — MVP 금지 (CLAUDE.md).
- **Multi-pointer / touch multi-finger** — `PointerEvent::isPrimary==true` 만 처리.
- **Hover propagation / bubbling 정책** — 본 iteration 은 최상위 hit 노드 한 개만 `interaction.hovered_id` 로 노출. 부모 체인 expose 는 future.
- **Live-reload dev-loop from `.iteration-6-criteria.md`** — 그 파일은 디스크에 유지되며 Iter 8 로 이동 (사전 합의).
- **Resolve-scene memoization microbench** — TODO 만 README/comment 에 남기고 측정은 optional.
- **WebGL2 fallback, 새 렌더러 backend** — 금지.
- **CEL 의 custom function 등록** — 기본 built-in + 변수 바인딩 만.
- **Scene-graph 수준 refcounted node sharing** — positional fallback 으로 충분.

---

## Acceptance criteria

### 1. `sdui-core` — `user_key` + stable NodeId + hit_test

- AC-1.1: `grep -E '^\s*pub user_key: Option<String>' sdui-core/src/lib.rs | wc -l` ≥ 3 (Frame, Text, Image 각각).
- AC-1.2: `grep -E 'user_key' sdui-core/src/lib.rs` 결과 `SceneNode::Condition` variant 정의 블록에는 **나타나지 않음** (Condition 에 필드 추가 금지).
- AC-1.3: `resolve_scene` 의 Frame clone 블록 (iteration 5 기준 lib.rs:281-291) 의 새 버전이 `user_key: f.user_key.clone(),` 라인을 포함. `grep -n 'user_key: f.user_key.clone' sdui-core/src/lib.rs | wc -l` ≥ 1.
- AC-1.4: `pub fn resolve_scene<E: ExpressionEngine>(root: &SceneNode, engine: &E, bindings: &ExprBindings) -> Option<SceneNode>` 시그니처 존재. `grep -nE 'pub fn resolve_scene<E: ExpressionEngine>\(root: &SceneNode, engine: &E, bindings: &ExprBindings\)' sdui-core/src/lib.rs | wc -l` == 1.
- AC-1.5: 하위 호환 래퍼 `resolve_scene_no_bindings` 존재. `grep -nE 'pub fn resolve_scene_no_bindings' sdui-core/src/lib.rs | wc -l` == 1. 내부는 `resolve_scene(root, engine, &ExprBindings::empty())` 한 줄 위임.
- AC-1.6: `pub const ROOT_NODE_ID: NodeId = NodeId(0);` 라인이 계속 존재. `grep -n 'pub const ROOT_NODE_ID: NodeId = NodeId(0)' sdui-core/src/lib.rs | wc -l` == 1.
- AC-1.7: `flatten_recursive` 가 positional `next_id: &mut u64` 파라미터 대신 `parent_path_hash: u64` 를 받는 시그니처로 전환. `grep -nE 'fn flatten_recursive\([^)]*parent_path_hash: u64' sdui-core/src/lib.rs | wc -l` == 1.
- AC-1.8: `hit_test_scene` 공개 함수 존재 — `grep -nE 'pub fn hit_test_scene\(flat: &\[FlatNode\], x: f32, y: f32\) -> Option<NodeId>' sdui-core/src/lib.rs | wc -l` == 1.
- AC-1.9: `hit_test_scene` 이 reverse iteration — 소스 내 `flat.iter().rev()` 또는 `(0..flat.len()).rev()` 패턴이 본체에 등장. `grep -nE 'flat.iter\(\).rev\(\)|\(0\.\.flat\.len\(\)\)\.rev\(\)' sdui-core/src/lib.rs | wc -l` ≥ 1.
- AC-1.10: 새 test 파일 `sdui-core/tests/identity.rs` 존재. `test flatten_preserves_id_across_sibling_insertion` 이 **포함**. `cargo test -p sdui-core --test identity flatten_preserves_id_across_sibling_insertion` exit 0.
- AC-1.11: 동일 test 파일에 두 번째 케이스 `positional_fallback_ids_are_deterministic` 포함 — 같은 키 없는 scene 을 두 번 flatten 하면 모든 NodeId 가 동일함을 assert. exit 0.
- AC-1.12: 새 `hit_test` test — `sdui-core/tests/hit_test.rs` 또는 `mod tests` 내부. 최소 3 케이스: inside child, inside parent only, outside root. 모두 `cargo test -p sdui-core` 로 exit 0.
- AC-1.13: `cargo test -p sdui-core` exit 0 — 기존 iter 5 의 12 개 + 신규 케이스 모두 pass.
- AC-1.14: `cargo clippy -p sdui-core --all-targets -- -D warnings` stderr 에 `error:` 0 건.
- AC-1.15: scene JSON 에 `user_key` 를 **넣지 않은** 기존 `examples/smoke/scene.json` 이 serde 로 여전히 parse 됨 — `cargo test -p sdui-core scene_node_json_round_trip` exit 0 및 smoke scene 의 wire round-trip 테스트 추가 권장 (기존 테스트로 커버되면 중복 생략 가능).
- AC-1.16: `flatten_recursive` 는 루트가 아닌 노드에 대해 `NodeId(0)` 를 반환해선 안 된다. 두 evidence:
  - (a) 구현에 충돌 가드 존재 — `grep -nE 'if (id|hashed|h) == 0' sdui-core/src/lib.rs | wc -l` ≥ 1 **or** `grep -nE '\.max\(1\)' sdui-core/src/lib.rs | wc -l` ≥ 1. 두 관용 표현 중 하나 허용.
  - (b) 단위 테스트 — `sdui-core/tests/identity.rs` 에 `hash_never_produces_zero_for_keyed_nodes` (또는 `root_reserved_is_never_reused_for_child`) 가 존재하고 통과. `grep -nE 'fn hash_never_produces_zero|fn root_reserved_is_never_reused' sdui-core/tests/identity.rs | wc -l` ≥ 1, `cargo test -p sdui-core --test identity <testname>` exit 0. 근거: §T-2 의 "0 이면 1 로 치환" 약속이 AC 로 승격.

### 2. `sdui-cel` — variable binding + Program 재사용

- AC-2.1: `pub struct ExprBindings` 과 `pub enum ExprValue` 가 `sdui-cel/src/lib.rs` 에 선언. `grep -nE 'pub struct ExprBindings|pub enum ExprValue' sdui-cel/src/lib.rs | wc -l` ≥ 2.
- AC-2.2: `ExprBindings::empty() -> Self`, `with_string(self, &str, &str) -> Self`, `with_int(self, &str, i64) -> Self`, `with_bool(self, &str, bool) -> Self`, `with_null(self, &str) -> Self`, **`with_map(self, &str, HashMap<String, ExprValue>) -> Self`** 여섯 메서드 존재. `grep -nE 'fn (empty|with_string|with_int|with_bool|with_null|with_map)' sdui-cel/src/lib.rs | wc -l` ≥ 6.
- AC-2.3: `ExpressionEngine` trait 에 `fn eval_bool_with(&self, expr: &CompiledExpr, bindings: &ExprBindings) -> Result<bool, ExprError>;` 메서드 존재. `grep -n 'fn eval_bool_with' sdui-cel/src/lib.rs | wc -l` ≥ 2 (trait decl + CelEngine impl).
- AC-2.4: 기존 `fn eval_bool` 이 유지되며 **default implementation** 으로 `self.eval_bool_with(expr, &ExprBindings::empty())` 를 호출. `grep -nE 'self.eval_bool_with\(expr, &ExprBindings::empty\(\)\)' sdui-cel/src/lib.rs | wc -l` ≥ 1.
- AC-2.5: `CelEngine::eval_bool_with` 구현이 `cel_interpreter::Context` 에 변수를 설치 후 `Program::execute` 를 호출. `grep -nE 'add_variable|Context::default' sdui-cel/src/lib.rs | wc -l` ≥ 2. 또한 맵 바인딩 경로가 코드상 존재함을 `grep -nE 'Value::Map|Value::String|Value::Null' sdui-cel/src/lib.rs | wc -l` ≥ 2 로 증명.
- AC-2.6: `cargo test -p sdui-cel` exit 0. 최소 신규 테스트 케이스 이름 (`#[test] fn ...`) 이 소스에 존재:
  - `binds_string_variable_and_compares_equal`
  - `binds_string_variable_and_compares_not_equal`
  - `unbound_variable_reference_errors`
  - `legacy_eval_bool_still_works_without_bindings`
  - `bindings_builder_api_is_fluent`
  - `null_binding_compares_unequal_to_string_literal`
  - **`binds_map_and_field_access_works`** — `ExprBindings::empty().with_map("interaction", HashMap::from([("hovered_id".into(), ExprValue::String("btn1".into()))]))` + 표현식 `interaction.hovered_id == 'btn1'` → `Ok(true)`.
  - `grep -nE 'fn (binds_string_variable|unbound_variable|legacy_eval_bool|bindings_builder|null_binding|binds_map_and_field_access)' sdui-cel/src/lib.rs | wc -l` ≥ 7.
- AC-2.7: `cargo clippy -p sdui-cel --all-targets -- -D warnings` stderr 에 `error:` 0 건.
- AC-2.8: **Program 재사용 문서 진술** — `sdui-cel/src/lib.rs` 파일 top-level doc comment 또는 `CompiledExpr` doc 에 "reusable across evaluations" 가 명시. `grep -nE 'reusable|reuse|reused' sdui-cel/src/lib.rs | wc -l` ≥ 1.
- AC-2.9: `ExprValue::Null`, `ExprValue::Map` variant 존재 — `grep -nE 'enum ExprValue' sdui-cel/src/lib.rs -A 10 | grep -cE 'Null|Map'` ≥ 2.

### 3. `sdui-runtime-wgpu` — byte-identical

- AC-3.1: `git diff sdui-runtime-wgpu/ | wc -l` == 0 (iteration 6 이 defer 됐으므로 iteration 5 확정본 대비).
- AC-3.2: `cargo check -p sdui-runtime-wgpu` exit 0; `cargo clippy -p sdui-runtime-wgpu -- -D warnings` stderr 에 `error:` 0 건.

### 4. `app-native` — pointer plumbing + resolve-on-hover + JSONL sidecar

- AC-4.1: `app-native/src/main.rs` 에 `WindowEvent::CursorMoved` arm 이 존재 — `grep -nE 'WindowEvent::CursorMoved' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.2: `WindowEvent::CursorLeft` arm 또한 존재 — `grep -nE 'WindowEvent::CursorLeft' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.3: `App` struct 에 `hovered_id: Option<NodeId>` 필드. `grep -nE 'hovered_id: Option<NodeId>' app-native/src/main.rs | wc -l` == 1.
- AC-4.4: `App` 이 `raw_scene: SceneNode` (unresolved, hover 변경 시 재 resolve 의 입력) 과 `resolved_scene: SceneNode` 를 분리 보관. `grep -nE '(raw_scene|resolved_scene): SceneNode' app-native/src/main.rs | wc -l` ≥ 2.
- AC-4.5: hover 변경 경로 — `resolve_scene(&self.raw_scene, &self.engine, &bindings)` 재호출 후 `flatten_scene` 재생성 후 `adapter.update_if_active(...)` + `window.request_redraw()`. `grep -nE 'resolve_scene\(&self\.raw_scene' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.6: JSONL 사이드카 write 경로 — `std::env::var("FABRICA_HOVER_LOG")` 참조 + `OpenOptions::new().create(true).append(true)` 사용. `grep -nE 'FABRICA_HOVER_LOG' app-native/src/main.rs | wc -l` ≥ 1. `grep -nE 'OpenOptions::new\(\)\.create\(true\)\.append\(true\)' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.7: JSONL line schema 가 `{"tick":<u64>,"kind":"hover_enter"|"hover_leave","node_id":<u64>,"user_key":<string>|null}` 임. 단위 테스트 또는 integration test 가 sample transition 으로 이 key 집합을 assert. 테스트 케이스 이름 `jsonl_line_contains_required_keys` 가 소스에 등장 — `grep -n 'fn jsonl_line_contains_required_keys' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.8: JSONL file write 는 매 라인마다 `writeln!(file, ...)` + `file.flush()` 또는 동등한 즉시-flush 경로 포함 — `grep -nE 'flush\(\)' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.9: hit-test 가 `sdui_core::hit_test_scene` 을 호출 — `grep -nE 'hit_test_scene\(' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.10: `NodeId → user_key` 역맵을 `flatten_scene` 결과에서 빌드하는 헬퍼가 존재 — `grep -nE 'fn build_user_key_map|HashMap<NodeId, Option<String>>' app-native/src/main.rs | wc -l` ≥ 1.
- AC-4.11: `cargo test -p app-native` exit 0. 최소 unit tests: hover_state_transition_emits_enter_and_leave, no_log_when_env_var_missing, user_key_is_null_for_unkeyed_hit.
- AC-4.12: `cargo clippy -p app-native --all-targets -- -D warnings` stderr 에 `error:` 0 건.
- AC-4.13: Iteration 6 의 live-reload 관련 기능이 아직 구현되지 **않았음** 을 확인 — `grep -nE 'notify_debouncer_full|UserEvent::ReloadScene' app-native/src/main.rs | wc -l` == 0. (iter 6 deferred; iter 7 은 이 기능 요구 없음.)

### 5. `app-web` — pointer plumbing + mirror DOM hover class + JS log buffer

- AC-5.1: `app-web/Cargo.toml` 의 `web-sys` features 에 `PointerEvent` 와 `EventTarget` 이 추가. `grep -nE '"PointerEvent"|"EventTarget"' app-web/Cargo.toml | wc -l` ≥ 2.
- AC-5.2: `app-web/src/lib.rs` 에 canvas 에 `pointermove` / `pointerleave` 리스너를 attach 하는 코드 존재 — `grep -nE 'add_event_listener_with_callback\("pointermove"|"pointerleave"' app-web/src/lib.rs | wc -l` ≥ 2.
- AC-5.3: `PointerEvent::client_x/client_y` 를 캔버스의 `getBoundingClientRect()` 로 보정 — `grep -nE 'get_bounding_client_rect|client_x|client_y' app-web/src/lib.rs | wc -l` ≥ 3.
- AC-5.4: 포인터 좌표 → `hit_test_scene` → `hovered_id` 경로 존재 — `grep -nE 'hit_test_scene\(' app-web/src/lib.rs | wc -l` ≥ 1.
- AC-5.5: hover 변경 시 `resolve_scene(&raw, &engine, &bindings)` 재호출 + mirror DOM 재마운트 경로 존재 — `grep -nE 'resolve_scene\([^)]*bindings' app-web/src/lib.rs | wc -l` ≥ 1.
- AC-5.6: 이전 hover 노드의 mirror `<div>` 에 `class="hovered"` 제거, 새 hover 노드에 부여 — `grep -nE 'class_name|set_class_name|"hovered"' app-web/src/lib.rs | wc -l` ≥ 2.
- AC-5.7: JS side log buffer 초기화 — `window.__fabrica_hover_log = []` 를 `app-web/static/index.html` 또는 `app-web/src/lib.rs` 초기화 경로에서 세팅. `grep -rnE '__fabrica_hover_log' app-web/ | wc -l` ≥ 1.
- AC-5.8: hover 전이마다 `window.__fabrica_hover_log.push({...})` + `console.log(JSON.stringify(...))` 가 실행 — `grep -nE '__fabrica_hover_log.push|console.log' app-web/src/lib.rs | wc -l` ≥ 1.
- AC-5.9: `cargo build -p app-web --target wasm32-unknown-unknown` exit 0.
- AC-5.10: `cargo clippy -p app-web --target wasm32-unknown-unknown -- -D warnings` stderr 에 `error:` 0 건.
- AC-5.11: 기존 smoke 의 web 경로 회귀 없음 — AC-11 참조.

### 6. `sdui-core::resolve_scene` 시그니처 전파

- AC-6.1: 모든 `resolve_scene(...)` 호출 사이트가 3-인자 형태 (`root, engine, bindings`) 또는 `resolve_scene_no_bindings(...)` 중 하나로 컴파일. `cargo check --workspace` exit 0.
- AC-6.2: `app-native` / `app-web` 에서 hover-demo 경로는 3-인자를 사용. smoke 경로는 `resolve_scene_no_bindings` 또는 `ExprBindings::empty()` 전달 — 기존 동작 보존.
- AC-6.3: `grep -nE 'resolve_scene(_no_bindings)?\(' sdui-core/src/lib.rs app-native/src/main.rs app-web/src/lib.rs | wc -l` ≥ 4 (최소한 3 파일 중 여러 호출).

### 7. `examples/hover-demo/`

- AC-7.1: `examples/hover-demo/scene.json` 존재. 최상위 Frame 내부에 `user_key: "btn1"` 을 가진 child Frame 존재. `jq '.. | objects | select(.user_key == "btn1")' examples/hover-demo/scene.json | head -c 1 | wc -c` ≥ 1.
- AC-7.2: scene JSON 에 `Condition` 노드 1 개 이상 존재하며 `when` 식이 `interaction.hovered_id == 'btn1'` 같은 bare-identifier 참조를 포함 (CEL spec §Syntax 에 따라 `$` 접두는 금지: <https://github.com/google/cel-spec/blob/master/doc/langdef.md#syntax>). `grep -E 'interaction.hovered_id' examples/hover-demo/scene.json | wc -l` ≥ 1. `grep -c '\$interaction' examples/hover-demo/scene.json` == 0.
- AC-7.3: `examples/hover-demo/README.md` 존재, 아래 3 섹션 포함: "Manual hover test (native)", "Manual hover test (web)", "JSONL sidecar inspection". `grep -E '^##' examples/hover-demo/README.md | wc -l` ≥ 3.
- AC-7.4: `examples/hover-demo/golden/native.png`, `examples/hover-demo/golden/web.png` — 초기 hover-off 상태만 캡처. 두 파일 존재 여부 `ls examples/hover-demo/golden/native.png examples/hover-demo/golden/web.png` exit 0.
- AC-7.5: `examples/smoke/scene.json` 은 unchanged — `git diff examples/smoke/scene.json | wc -l` == 0.
- AC-7.6: `examples/condition-panel/` 디렉터리는 **존재하지 않는다** — `test ! -d examples/condition-panel` exit 0. 이 AC 는 Iteration 6 이 본 iteration 과 함께 실수로 구현되지 않았음을 검증한다. `.iteration-6-criteria.md` 가 디스크에 남아 있어도 그것은 deferred plan 문서일 뿐 구현이 아니므로 디렉터리는 존재하지 않아야 한다.

### 8. `xtask`

- AC-8.1: `cargo xtask run-native --example hover-demo --background --pidfile $P` exit 0, 창 띄움.
- AC-8.2: `cargo xtask run-web --example hover-demo --port 8141 --background --pidfile $P2` exit 0, `curl -sf http://localhost:8141/examples/hover-demo/` 또는 index 경로 200.
- AC-8.3: dist 에 scene.json 복사 — `curl -sf http://localhost:8141/scene.json` 이 200 + valid JSON.
- AC-8.4: `cargo xtask build-all` exit 0.

### 9. Verification — native hover probe

- AC-9.1: `.claude/skills/verify-native/SKILL.md` 에 **Step: hover probe** 섹션 추가. 절차:
  1. Preflight (mock-server build + health + asset probe — CLAUDE.md §Preflight).
  2. `OUT=target/verify-out`; `mkdir -p $OUT`; `LOG=$OUT/hover-events.jsonl`; `rm -f $LOG`.
  3. `FABRICA_HOVER_LOG=$LOG cargo xtask run-native --example hover-demo --background --pidfile $OUT/app.pid`.
  4. 창 뜰 때까지 대기 + `cargo xtask inspect-ax --pid $(cat $OUT/app.pid) > $OUT/ax-before.json`.
  5. AX tree 에서 `user_key == "btn1"` 이 걸린 노드의 bounds 획득 (혹은 AX bounds + screen offset 으로 center 계산). 대안: golden PNG 내 btn1 bounds 상수를 `examples/hover-demo/README.md` 에 문서화 + verifier 가 그 상수 사용.
  6. `cliclick m:<x>,<y>` 로 포인터 이동 (§T-10).
  7. `sleep 0.5`.
  8. `tail -n 1 $LOG` 가 유효 JSON + `.kind == "hover_enter"` + `.user_key == "btn1"`. `jq -e '.kind == "hover_enter" and .user_key == "btn1"' <<< "$(tail -n 1 $LOG)"` exit 0.
  9. `cliclick m:0,0` (캔버스 바깥 영역).
  10. `sleep 0.5`.
  11. `tail -n 1 $LOG` 가 `{"kind":"hover_leave", ...}`. `jq -e '.kind == "hover_leave"' <<< "$(tail -n 1 $LOG)"` exit 0.
  12. Post-state AX dump: `cargo xtask inspect-ax --pid $(cat $OUT/app.pid) > $OUT/ax-after.json`. AX tree 무결성.
  13. stderr grep `panic|wgpu error|cel parse failed|cel eval failed` 0 건.
  14. Teardown: `kill $(cat $OUT/app.pid)`.
- AC-9.2: `cliclick` 의 부재 (`command -v cliclick` fail) 를 skill 이 명시적으로 감지하고 사용자에게 설치 안내 (`brew install cliclick`) — skill 내부 가드 구문이 존재. `grep -n 'cliclick' .claude/skills/verify-native/SKILL.md | wc -l` ≥ 2.
- AC-9.3: JSONL 파일의 각 라인이 AC-4.7 의 스키마를 만족함을 verifier 가 `jq -c '.' $LOG | while read L; do jq -e '.tick and .kind and .node_id and (.user_key or .user_key==null)' <<<"$L" >/dev/null || exit 1; done` 로 확인. 모든 라인 exit 0.
- AC-9.4: Log line 개수 ≥ 2 (hover_enter + hover_leave 최소). `wc -l < $LOG` ≥ 2.
- AC-9.5: skill 이 reload probe (iter 6 의 live-reload) 를 언급하지 않음. `grep -n 'Live-reload probe\|ReloadScene' .claude/skills/verify-native/SKILL.md | wc -l` == 0.

### 10. Verification — web hover probe

- AC-10.1: `.claude/skills/verify-web/SKILL.md` 에 **Step: hover probe** 섹션 추가. 절차:
  1. Preflight.
  2. `cargo xtask run-web --example hover-demo --port 8141 --background --pidfile $OUT/server.pid`.
  3. `agent-browser` → navigate → `div[role="application"]` 대기.
  4. `window.__fabrica_hover_log = []` 가 존재 — 페이지 초기화로 이미 세팅된 값. `typeof window.__fabrica_hover_log === 'object'` assertion.
  5. btn1 의 mirror `<div>` 를 `data-user-key="btn1"` 로 조회 (mount 시 attribute 를 부여 — §T-8).
  6. 해당 element 위 좌표로 `new PointerEvent('pointermove', {clientX:..., clientY:..., bubbles:true, isPrimary:true})` 를 **canvas** 에 dispatch (mirror DOM 이 아닌 canvas 가 리스너를 갖고 있음).
  7. `await new Promise(r=>setTimeout(r,100))`.
  8. Assert `document.querySelector('[data-user-key="btn1"]').className.includes('hovered')`.
  9. Assert `window.__fabrica_hover_log.at(-1).kind === 'hover_enter'` 및 `.user_key === 'btn1'`.
  10. canvas 바깥 좌표로 `pointerleave` dispatch → `setTimeout 100` → assert class 미포함 + log 끝줄 `hover_leave`.
  11. Console `ERROR|panic|wgpu|CORS` 0 건.
  12. Teardown: `kill $(cat $OUT/server.pid)`.
- AC-10.2: mirror DOM 에 `data-user-key` attribute 를 키 보유 노드에만 부여. `grep -nE 'set_attribute\("data-user-key"' app-web/src/lib.rs | wc -l` ≥ 1.
- AC-10.3: `window.__fabrica_hover_log` 배열의 각 원소가 AC-4.7 schema 를 따름 — agent-browser 가 `log.every(l => typeof l.tick === 'number' && (l.kind === 'hover_enter' || l.kind === 'hover_leave') && typeof l.node_id === 'number' && (typeof l.user_key === 'string' || l.user_key === null))` assertion.
- AC-10.4: `grep -n 'Live-reload\|textarea' .claude/skills/verify-web/SKILL.md | wc -l` == 0 — iter 6 경로 없음.

### 11. Regression — Iteration 5 smoke 재현

- AC-11.1: `/verify-native smoke` exit 0 — iteration 5 의 AC 모두 pass.
- AC-11.2: `/verify-web smoke` exit 0 — 동일.
- AC-11.3: `jq '[.. | objects | select(.role=="image")] | length' target/verify-out/ax-dump-*.json` == 1 (smoke scene 의 단일 이미지).
- AC-11.4: smoke golden PNG 재사용 — 시각 회귀 없음.
- AC-11.5: `resolve_scene_no_bindings` (혹은 empty bindings) 경로로 기존 scene 이 동작 — `cargo test -p sdui-core` 에 포함되는 기존 condition 관련 테스트 모두 pass.

### 12. Preflight + 전체 toolchain green

- AC-12.1: 매 verify 실행 앞에 mock-server preflight 수행 (CLAUDE.md §Preflight).
- AC-12.2: `cargo fmt --all -- --check` exit 0.
- AC-12.3: `cargo check --workspace` exit 0.
- AC-12.4: `cargo clippy --workspace --all-targets -- -D warnings` stderr 에 `error:` 0 건.
- AC-12.5: `cargo test --workspace` exit 0.
- AC-12.6: `cargo xtask build-all` exit 0.
- AC-12.7: `cargo build -p app-web --target wasm32-unknown-unknown` exit 0.

---

## Technical decisions

### T-1. `user_key: Option<String>` — Frame / Text / Image only

**Decision:** `SceneNode::Frame`, `SceneNode::Text`, `SceneNode::Image` 세 variant 에만 `user_key: Option<String>` 을 추가. `SceneNode::Condition` 에는 추가하지 않는다.

- 근거: `Condition` 은 `resolve_scene` 이 **삭제** 하거나 child 로 치환하는 operator 이다. flatten 단계에 도달하면 `unreachable!` 이므로 ID 대상이 아니다.
- serde: `#[serde(default, skip_serializing_if = "Option::is_none")]` — 기존 JSON 이 필드 없이도 parse 되어야 한다.
- Evidence:
  - serde 옵션 필드 패턴: <https://serde.rs/field-attrs.html#skip_serializing_if>

### T-2. NodeId 안정화 — 경로 해시

**Decision:** `flatten_recursive` 가 ID 를 `DefaultHasher::new()` 기반으로 생성. 부모 경로 해시 `parent_path_hash` 를 누적 상태로 받고, 현재 노드 세그먼트는 `(":k:" + user_key)` 또는 `(":i:" + sibling_index)` 를 hasher 에 feed → `finish()` 결과가 현재 노드 ID (nonzero 보장을 위해 0 이면 1 로 치환). `ROOT_NODE_ID = NodeId(0)` 예약 유지.

- Alternatives A/B/C 기각 (siphash 의존 추가 / positional 유지 / UUID fallback) — 기본 해시 + 프로세스 내 결정론으로 충분.
- Hasher 재사용: 각 노드마다 `let mut h = DefaultHasher::new(); h.write_u64(parent_path_hash); h.write(segment_bytes); h.finish()`. 자식의 parent_path_hash 는 현재 노드 ID 의 finish 값.
- Evidence:
  - `DefaultHasher`: <https://doc.rust-lang.org/std/collections/hash_map/struct.DefaultHasher.html>
  - `Hasher::finish()`: <https://doc.rust-lang.org/std/hash/trait.Hasher.html#tymethod.finish>
  - `DefaultHasher` cross-process randomness: <https://doc.rust-lang.org/std/collections/struct.RandomState.html>

### T-3. Hit-test 배치 — `sdui-core::hit_test` 모듈

**Decision:** `sdui-core` 내부에 `pub fn hit_test_scene(flat: &[FlatNode], x: f32, y: f32) -> Option<NodeId>` 추가. 별도 crate 미도입.

- Algorithm: `flat.iter().rev().find(|n| n.id != ROOT_NODE_ID && x >= n.abs_x && x < n.abs_x + n.width && y >= n.abs_y && y < n.abs_y + n.height).map(|n| n.id)`.
- Painter's order: `flatten_scene` depth-first pre-order → `rev()` = top-most first.
- Evidence: AABB containment: <https://developer.mozilla.org/en-US/docs/Web/CSS/CSSOM_view/Coordinate_systems>.

### T-4. CEL variable binding — `ExprBindings` + `eval_bool_with`

**Decision:** `sdui-cel` 에 `ExprBindings` (name → `ExprValue`) 빌더 + `eval_bool_with`. `cel_interpreter::Context::default()` 에 각 binding 설치.

- `ExprValue::Map(HashMap<String, ExprValue>)` 지원으로 `interaction.hovered_id` 같은 field access 가능. runtime: `ExprBindings::empty().with_map("interaction", HashMap::from([("hovered_id".into(), hover_val)]))`.
- Scene JSON 의 CEL 식은 `interaction.hovered_id == 'btn1'` (bare identifier; `$` 접두 없음).
- Evidence:
  - `cel-interpreter::Context::add_variable`: <https://docs.rs/cel-interpreter/0.10/cel_interpreter/context/enum.Context.html#method.add_variable>
  - `cel_interpreter::Value`: <https://docs.rs/cel-interpreter/0.10/cel_interpreter/objects/enum.Value.html>
  - CEL syntax (bare identifiers): <https://github.com/google/cel-spec/blob/master/doc/langdef.md#syntax>
  - CEL field selection: <https://github.com/google/cel-spec/blob/master/doc/langdef.md#field-selection>

### T-5. Compiled-expression caching — call-site responsibility

**Decision:** `CompiledExpr` 는 재사용 가능 (`cel_interpreter::Program` wrap). `resolve_scene` 내부는 매 호출 compile 허용 (iter 7 scope 는 식별성/hover). 호출자-측 `HashMap<String, CompiledExpr>` 캐시는 iter 8+ 과제.

- doc comment 에 "reusable across evaluations, callers may cache" 명시 (AC-2.8).
- Evidence: `cel_interpreter::Program::execute` multi-call: <https://docs.rs/cel-interpreter/0.10/cel_interpreter/struct.Program.html#method.execute>

### T-6. Winit pointer plumbing — `CursorMoved` / `CursorLeft`

**Decision:** winit 0.30 `WindowEvent::CursorMoved { position, .. }` (PhysicalPosition) → `window.scale_factor()` 로 logical coord 변환 → f32.

- `CursorLeft` → hovered_id = None.
- single mouse only; touch / multi-pointer 배제.
- Evidence:
  - CursorMoved: <https://docs.rs/winit/0.30/winit/event/enum.WindowEvent.html#variant.CursorMoved>
  - CursorLeft: <https://docs.rs/winit/0.30/winit/event/enum.WindowEvent.html#variant.CursorLeft>
  - scale_factor: <https://docs.rs/winit/0.30/winit/window/struct.Window.html#method.scale_factor>

### T-7. resolve-on-hover cadence — 매 hover change 마다 full resolve

**Decision:** `hovered_id` 변경 시마다 `resolve_scene(&raw, &engine, &bindings)` 전체 재실행. diff 기반 부분 업데이트 없음. AccessKit `TreeUpdate` 도 full push.

- 근거: scene 작고 (≤ 수십 노드), hover 변경 빈도 ≈ 60Hz. 전체 O(n) resolve + flatten + TreeUpdate 가 16ms 안에 들어감.
- `resolve_scene` 시그니처 확장 — bindings 파라미터. 모든 기존 호출자는 `resolve_scene_no_bindings` 또는 `ExprBindings::empty()` 마이그레이션.
- Evidence:
  - AccessKit TreeUpdate: <https://docs.rs/accesskit/0.24/accesskit/struct.TreeUpdate.html>

### T-8. Web pointer plumbing — canvas `pointermove`/`pointerleave`

**Decision:** `<canvas>` 에 리스너. `clientX - canvasRect.left` 등으로 scene 좌표. mirror DOM 은 ARIA only (pointer-events: none 권장).

- `data-user-key="{key}"` attribute 를 mirror DOM 의 키 보유 노드에 부여 (AC-10.2).
- `PointerEvent::isPrimary === true` 만 처리.
- Evidence:
  - PointerEvent: <https://developer.mozilla.org/en-US/docs/Web/API/PointerEvent>
  - getBoundingClientRect: <https://developer.mozilla.org/en-US/docs/Web/API/Element/getBoundingClientRect>

### T-9. JSONL sidecar — permanent hover verification channel

**Decision:** Runtime 이 hover_enter / hover_leave 전이마다 한 줄 JSON append.
- Native: `FABRICA_HOVER_LOG=<path>` env var gate. `OpenOptions::new().create(true).append(true).open(path)` + `writeln!` + `flush()`.
- Web: `console.log(JSON.stringify(line))` + `window.__fabrica_hover_log.push(line)`.
- Line schema:
  ```json
  {"tick": 42, "kind": "hover_enter", "node_id": 18446744073709551613, "user_key": "btn1"}
  ```
- `tick`: monotonic u64, 프로세스 시작 0.
- `node_id`: 해시 기반 u64.
- `user_key`: `Some(String)` → JSON string, `None` → `null`.
- Evidence:
  - OpenOptions: <https://doc.rust-lang.org/std/fs/struct.OpenOptions.html>
  - AccessKit macOS hover 공백: <https://github.com/AccessKit/accesskit/blob/main/platforms/macos/CHANGELOG.md>

### T-10. Native pointer automation — `cliclick`

**Decision:** `cliclick m:x,y` 를 `/verify-native` 포인터 이동 수단. AppleScript System Events 에는 mousemove primitive 없음.

- Skill 가드: "requires `cliclick` — `brew install cliclick`" (AC-9.2).
- Coordinate: screen-global. window position 보정 필요.
- Evidence:
  - cliclick: <https://github.com/BlueM/cliclick>
  - AppleScript UI automation: <https://developer.apple.com/library/archive/documentation/LanguagesUtilities/Conceptual/MacAutomationScriptingGuide/WorkwithUIElements.html>

### T-11. Regression-first test ordering — RED before GREEN

**Decision:** `sdui-core/tests/identity.rs` 의 `flatten_preserves_id_across_sibling_insertion` 은 IMP 가 **RED 상태로 commit** 후 구현 commit 으로 GREEN 전환.

- 순서:
  1. Commit A: 테스트 파일만 추가 — FAIL.
  2. Commit B: `user_key` + hash-based flatten — PASS.
- VER 가 `git log -p` 로 RED→GREEN 확인.
- Evidence: Kent Beck, *Test-Driven Development: By Example* (2002), Ch. 1.

### T-12. Verification skill — hover probe mandatory step

**Decision:** 두 skill 문서에 **Step: hover probe** 독립 섹션. iter 6 의 Live-reload probe 는 추가하지 않음 (iter 6 deferred).

- Native: cliclick 가드, JSONL tail + jq, AX diff.
- Web: `data-user-key` selector, PointerEvent dispatch, `window.__fabrica_hover_log` assert.
- 두 skill 모두 preflight 전제.

### T-13. Docs housekeeping — iter 6 defer 기록

**Decision:**
- README.md table 에 Iter 7 entry + hover-demo 링크.
- SPRINTS.md 에 "Iteration 7: Complete" + "Iteration 6: live-reload dev-loop — deferred, criteria retained at `.iteration-6-criteria.md`".
- CLAUDE.md "Repository status" → "Through Iteration 7 ..." + 2 문장 hover/JSONL 설명.

---

## Files modified in the planned iteration

1. `sdui-core/src/lib.rs` — `user_key` 필드, `resolve_scene` 시그니처, `resolve_scene_no_bindings`, `flatten_recursive` hash ID, `pub fn hit_test_scene`.
2. `sdui-core/tests/identity.rs` — 신규.
3. `sdui-core/tests/hit_test.rs` — 신규 또는 mod tests 확장.
4. `sdui-cel/src/lib.rs` — `ExprBindings`, `ExprValue`, `eval_bool_with`, default `eval_bool` 위임, 7 유닛 테스트.
5. `app-native/Cargo.toml` — 추가 의존 없음.
6. `app-native/src/main.rs` — CursorMoved/Left, hovered_id, hit_test, bindings, resolve/flatten 재실행, JSONL writer, 유닛 테스트.
7. `app-web/Cargo.toml` — web-sys `PointerEvent`, `EventTarget` 등 추가.
8. `app-web/src/lib.rs` — listener, getBoundingClientRect, hit_test, bindings, resolve, mirror DOM `hovered` class + `data-user-key`, JS log buffer.
9. `app-web/static/index.html` — `window.__fabrica_hover_log = []` 초기화.
10. `examples/hover-demo/scene.json` — 신규.
11. `examples/hover-demo/README.md` — 신규.
12. `examples/hover-demo/golden/native.png`, `examples/hover-demo/golden/web.png` — 신규.
13. `xtask/src/main.rs` — hover-demo routing (기존 `--example` 로직 동작 시 변경 없음).
14. `.claude/skills/verify-native/SKILL.md` — Step: hover probe, cliclick 가드.
15. `.claude/skills/verify-web/SKILL.md` — Step: hover probe, agent-browser 절차.
16. `CLAUDE.md` — Repository status + hover/JSONL 문단.
17. `SPRINTS.md` — Iter 7 Complete + Iter 6 deferred 주석.
18. `README.md` — Iter 7 행 + hover-demo 링크.

(`.iteration-6-criteria.md` 는 **삭제하지 않는다**.)

---

## Verification plan

### `/verify-native`

1. Preflight (CLAUDE.md §Preflight): mock-server build + `/health` + `/assets/smoke.png` 200 image/png.
2. Smoke regression (AC-11.1): `cargo xtask run-native --example smoke --background --pidfile $P` → AX dump → image count == 1 + clean stderr.
3. Hover-demo baseline: `FABRICA_HOVER_LOG=$OUT/hover-events.jsonl cargo xtask run-native --example hover-demo --background --pidfile $P2` → inspect-ax before → screenshot vs golden.
4. Hover probe (AC-9.1): cliclick btn1 center → sleep 0.5 → tail JSONL → hover_enter + user_key=btn1 → cliclick outside → sleep 0.5 → hover_leave → AX 무결성 재확인.
5. Schema check (AC-9.3): 모든 JSONL line 이 4 키 보유.
6. stderr grep 0.
7. Teardown.

### `/verify-web`

1. Preflight.
2. Smoke regression (AC-11.2): `cargo xtask run-web --example smoke --port 8137 --background --pidfile $P` → agent-browser → mirror tree dump → image==1, clean console.
3. Hover-demo baseline: `cargo xtask run-web --example hover-demo --port 8141 --background --pidfile $P2` → navigate → `div[role="application"]` 대기 → `data-user-key="btn1"` 존재 → screenshot vs golden.
4. Hover probe (AC-10.1):
   - `window.__fabrica_hover_log.length` 기록 (L0).
   - btn1 bounds → PointerEvent dispatch to canvas → setTimeout 100 → `hovered` class assert + log.at(-1)=hover_enter.
   - pointerleave → setTimeout 100 → class 제거 + hover_leave.
5. Schema check (AC-10.3).
6. Console clean.
7. Process tree: 새 cargo/rustc 없음.
8. Teardown.

### Golden artifacts

- `examples/smoke/golden/*` — iter 4/5 재사용.
- `examples/hover-demo/golden/native.png|web.png` — 본 iter 신규, **초기 hover-off 만**.

### Additional regression grep

- `git diff sdui-runtime-wgpu/ | wc -l` == 0.
- `grep -E 'pub fn resolve_scene<' sdui-core/src/lib.rs | wc -l` == 1.
- `grep -E 'resolve_scene_no_bindings' sdui-core/src/lib.rs | wc -l` ≥ 1.
- `grep -E 'ROOT_NODE_ID: NodeId = NodeId\(0\)' sdui-core/src/lib.rs | wc -l` == 1.
- `grep -E 'user_key' sdui-core/src/lib.rs | wc -l` ≥ 5.
- `grep -E 'hit_test_scene' app-native/src/main.rs app-web/src/lib.rs | wc -l` ≥ 2.
- `grep -E 'FABRICA_HOVER_LOG' app-native/src/main.rs .claude/skills/verify-native/SKILL.md | wc -l` ≥ 2.
- `grep -rE '__fabrica_hover_log' app-web/ | wc -l` ≥ 2.
- `test ! -d examples/condition-panel && echo ok` == ok.
- `test -f .iteration-6-criteria.md && echo ok` == ok (defer 유지).

---

## Post-completion notes (placeholder)

<!-- 집행 단계 채움. 예상:
- PC-1. DefaultHasher cross-process seed: 테스트가 "equality within a single process" 만 assert 하는지 재확인.
  Evidence: <https://doc.rust-lang.org/std/collections/struct.RandomState.html>
- PC-2. cel-interpreter 0.10 의 Context::add_variable 시그니처 + Value::Map 구성 방법 실측. `interaction.hovered_id` field access 가 실제 동작.
  Evidence: <https://docs.rs/cel-interpreter/0.10/cel_interpreter/objects/enum.Value.html#variant.Map>
- PC-3. winit CursorMoved::position 은 physical; scale_factor 보정 후 multi-monitor retina 검증.
  Evidence: <https://docs.rs/winit/0.30/winit/dpi/index.html>
- PC-4. cliclick 좌표계 — 멀티모니터에서 primary screen 기준 확인, screencapture window position 과 정합.
  Evidence: <https://github.com/BlueM/cliclick/blob/master/CHANGELOG.md>
- PC-5. JSONL tick u64 → JS Number safe integer (2^53) 경계.
- PC-6. AccessKit TreeUpdate 전체 push cost 측정 (optional microbench).
  Evidence: <https://docs.rs/accesskit/0.24/accesskit/struct.TreeUpdate.html>
- PC-7. Mirror DOM class 변경이 MutationObserver 로 관찰 가능한지 web 측 manual 확인.
- PC-8. iter 6 criteria 파일 리넘버 정책 — `.iteration-6-criteria.md` 유지 vs `.iteration-8-criteria.md` 리네임. SPRINTS.md 에 명시.
-->
